"use client";
import { useState } from "react";
import { gallery } from "./choir/data";
type ManagedMedia = { id: string; media_type: "Photos" | "Videos"; url: string; caption: string; category: string };
export default function MediaGallery({ managedMedia = [] }: { managedMedia?: ManagedMedia[] }) { const [media, setMedia] = useState("Photos"); const photos = managedMedia.filter((item) => item.media_type === "Photos"); const videos = managedMedia.filter((item) => item.media_type === "Videos"); return <><div className="tab-list"><button className={media === "Photos" ? "selected" : ""} onClick={() => setMedia("Photos")}>Photo gallery</button><button className={media === "Videos" ? "selected" : ""} onClick={() => setMedia("Videos")}>Video highlights</button></div>{media === "Photos" ? <div className="gallery-grid">{photos.length ? photos.map((item, index) => <article className={`gallery-card gallery-${(index % 3) + 1}`} key={item.id} style={{ backgroundImage: `linear-gradient(135deg, rgba(27,67,50,.75), rgba(90,24,154,.78)), url(${item.url})`, backgroundSize: "cover" }}><div><span>{item.category}</span><h3>{item.caption}</h3></div></article>) : gallery.map((item, index) => <article className={`gallery-card gallery-${index + 1}`} key={item[0]}><div><span>{item[1]}</span><h3>{item[0]}</h3></div></article>)}</div> : <div className="video-grid">{videos.length ? videos.map((item) => <div className="video-card" key={item.id}><iframe src={item.url} title={item.caption} allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowFullScreen /></div>) : <><div className="video-card"><iframe src="https://www.youtube.com/embed/YIMuTMsqZXw" title="Namirembe choir worship highlights" allowFullScreen /></div><div className="video-card"><iframe src="https://www.youtube.com/embed/6WiFaufNyLY" title="Namirembe choir highlights" allowFullScreen /></div></>}</div>}</>; }
