"use client";
import { useState } from "react";
import { gallery } from "./choir/data";
export default function MediaGallery() { const [media, setMedia] = useState("Photos"); return <><div className="tab-list"><button className={media === "Photos" ? "selected" : ""} onClick={() => setMedia("Photos")}>Photo gallery</button><button className={media === "Videos" ? "selected" : ""} onClick={() => setMedia("Videos")}>Video highlights</button></div>{media === "Photos" ? <div className="gallery-grid">{gallery.map((item, index) => <article className={`gallery-card gallery-${index + 1}`} key={item[0]}><div><span>{item[1]}</span><h3>{item[0]}</h3></div></article>)}</div> : <div className="video-grid"><div className="video-card"><iframe src="https://www.youtube.com/embed/YIMuTMsqZXw" title="Namirembe choir worship highlights" allowFullScreen /></div><div className="video-card"><iframe src="https://www.youtube.com/embed/6WiFaufNyLY" title="Namirembe choir highlights" allowFullScreen /></div></div>}</>; }
